/**
 * internal utilities - not exposed publicly
 *
 * these functions are only used internally by `parseSegments` and `useSegments`
 */

export { extractKeyValuePairs } from './extractKeyValuePairs'
export { extractSegmentsFromPathname } from './extractSegmentsFromPathname'
export { rebuildPathname } from './rebuildPathname'
